import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF2E7D32); // Eco Green
  static const Color primaryLight = Color(0xFFE8F5E9);
  static const Color primaryVariant = Color(0xFF1B5E20);
  static const Color secondary = Color(0xFF81C784);
  static const Color secondaryVariant = Color(0xFF388E3C);

  // Background colors
  static const Color background = Color(0xFFFAFAFA);
  static const Color surface = Colors.white;
  static const Color cardBackground = Colors.white;

  // Text colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Status colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);
  static const Color warning = Color(0xFFFF9800);
  static const Color info = Color(0xFF2196F3);

  // Border colors
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFBDBDBD);

  // Special colors
  static const Color sosRed = Colors.red;
  static const Color rideGreen = Color(0xFF4CAF50);
  static const Color bookingBlue = Color(0xFF2196F3);
}
